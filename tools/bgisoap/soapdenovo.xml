<tool id="soapdenovo" name="SOAPdenovo" version="1.0.0">
  <description>De novo assembly with short read sequences</description>
  <command interpreter="python">
    soapdenovo.py
      --command_select=$command_select
      --process_select=$process_select
      --config_file=$config_file
      --output_prefix=$output_prefix
      --output_edge=$output_edge
      --output_preGraphBasic=$output_preGraphBasic
      --output_preArc=$output_preArc
      --output_vertex=$output_vertex
      --output_contig=$output_contig
      --output_scafSeq=$output_scafSeq
  </command>
  <inputs>
    <param name="command_select" type="select" format="text" label="Select kmer version" 
    help="Support large kmer up to 127 to utilize long reads. Three version are provided.
Please notice that, with longer kmer, the quantity of nodes would decrease significantly, thus the memory consumption is usually smaller than double with shifted version."> 
        <label>Select kmer version </label>
        <option value="31mer">31mer Version</option>
        <option value="63mer">63mer Version</option>
        <option value="127mer">127mer Version</option>
    </param>
    <param name="process_select" type="select" format="text" label="Select the process">
	<option value="all">all</option>
        <option value="pregraph">pregraph</option>
<!--
        <option value="contig">contig</option>
        <option value="map">map</option>
        <option value="scaff">scaff</option>
-->
    </param>
    <param name="config_file" type="data" format="input" label="SOAPdenovo configuration file"/>
<!--    <param name="kmer" type="integer" format="input" label="Kmer size" />
-->
  </inputs>
  <outputs>
    <data format="tabular" name="output_prefix" />
    <data format="tabular" name="output_edge" />
    <data format="tabular" name="output_preGraphBasic" />
    <data format="tabular" name="output_preArc" />
    <data format="tabular" name="output_vertex" />
    <data format="tabular" name="output_contig" />
    <data format="tabular" name="output_scafSeq" />
  </outputs>
  <tests>
    <test>
      <output name="output_edge" file="test.edge"/>
      <output name="output_preGraphBasic" file="test.preGraphBasic"/>
      <output name="output_preArc" file="test.preArc" />
      <output name="output_vertex" file="test.vertex" />
  </test>

 </tests>
<help>

**What it does**

SOAPdenovo is a novel short-read assembly method that can build a de novo draft assembly for the human-sized genomes. The program is specially designed to assemble Illumina GA short reads. It creates new opportunities for building reference sequences and carrying out accurate analyses of unexplored genomes in a cost effective way.

For detailed information on SOAPdenovo, click here_.
 
.. _here: http://soap.genomics.org.cn/soapdenovo.html

</help>
</tool>
